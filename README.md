# Streamlit_Projects


In the README for your Streamlit project, explicitly mention:
To run the app, use the command:

bash
Copy code
streamlit run YanellyHernandez.py


Mention this in your README so users can run it after installing dependencies:

bash
Copy code
python setup.py
